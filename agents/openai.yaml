interface:
  display_name: "Dreamwoven Reality"
  short_description: "生成摄影主导与极端抽象上下二分图，并按需输出明信片版本"
  default_prompt: "Use $dreamwoven-reality to turn the uploaded photograph into the default two-panel artwork: a photographic-led partial abstraction above and an extreme semantic abstraction below. Use the postcard layout only when I explicitly request a postcard."
